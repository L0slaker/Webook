db:
  dsn: "root:root@tcp(localhost:13316)/webook"

redis:
  addr: "localhost:6379"

kafka:
  addrs:
    - "localhost:9094"

grpc:
  client:
    inter:
      name: "interactive"
      addr: "localhost:8090"
      secure: false
      threshold: 100
      etcdAddr: "localhost:12379"

etcd:
  endpoints:
    - "localhost:12379"